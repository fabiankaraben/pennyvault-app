import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Basic test to check if the app starts
    // In a real scenario, we'd mock services since we use Hive and SharedPreferences
  });
}
